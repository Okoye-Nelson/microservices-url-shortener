# Redirect Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redirect-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redirect-service
  template:
    metadata:
      labels:
        app: redirect-service
    spec:
      containers:
        - name: redirect-service
          image: itsbaivab/redirect-service:latest # BEST PRACTICE: Use a specific version tag like 'v1.0.0' instead of ':latest'.
          env:
            - name: DB_HOST
              value: postgres
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: POSTGRES_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: POSTGRES_PASSWORD
            - name: DB_NAME
              value: urlshortener
            - name: RABBITMQ_URL
              value: "amqp://user:pass@rabbitmq:5672/" # Placeholder, will be substituted
            - name: RABBITMQ_USER
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-credentials
                  key: RABBITMQ_DEFAULT_USER
            - name: RABBITMQ_PASS
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-credentials
                  key: RABBITMQ_DEFAULT_PASS
            - name: SERVICE_PORT
              value: "8002"
          ports:
            - containerPort: 8002
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 10
            periodSeconds: 5
---
# Redirect Service Service
apiVersion: v1
kind: Service
metadata:
  name: redirect-service
  namespace: default
spec:
  selector:
    app: redirect-service
  ports:
    - port: 8002
      targetPort: 8002